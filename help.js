// for node >= 4.0.0
// npm install -g livid

module.exports = `
    Usage
      $ livid hot                : fetch hot topics
      $ livid recent             : fetch recent topics

    Options
      -v, --version              : print version number
      -h, --help                 : print help info
`
